// Worker URL configuration
// Client: use relative URLs for same-origin API
// Server: use full URL since it runs on different domain
export const WORKER_URL = ''; // empty = relative URL (client-side)
export const WORKER_URL_SERVER = 'https://glianapay.com'; // full URL (server-side)
export const WORKER_HOST = '';
